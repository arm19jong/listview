<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jongzazaal.listview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SimpleListActivity"></activity>
        <activity android:name=".class_layout_2"></activity>
        <activity android:name=".CustomListViewActivity"></activity>
        <activity android:name=".alligator"></activity>
        <activity android:name=".goat"></activity>
    </application>

</manifest>